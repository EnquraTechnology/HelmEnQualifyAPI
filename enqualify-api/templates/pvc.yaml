apiVersion: v1
kind: PersistentVolumeClaim
metadata:
   name: enqualify-api-claim
   namespaces: enqualifyapp
spec:
  storageClassName: nfs-sc
  accessModes:
  - ReadWriteMany
  resources:
    requests:
       storage: 100Gi
